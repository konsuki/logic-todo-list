# 実装計画書: フロー表示の視覚的階層化 (Enclosed Flow Hierarchy)

## ステータス
- **進捗率**: 100% [DONE]
- **最終更新日**: 2026-04-28

## 完了済みタスク

### Phase 1: データ構造と描画エンジンの拡張 [DONE]
- [x] `treeLogic.js` の `getFlattenedFlow` を拡張し、親ノードをフラットなシーケンスから除外するフィルタリングを実装。
- [x] 階層の深さ (depth) を正しく保持し、包含関係を特定できるメタデータを付与。

### Phase 2: D3.js による包含フレーム (Enclosures) の実装 [DONE]
- [x] リーフノードの Bounding Box を計算し、親フレームを描画するロジックの実装。
- [x] 階層に応じた動的パディング (Hierarchical Padding) の導入。
- [x] 背景の透過度グラデーションによる深さの表現。

### Phase 3: プログレス連動とビジュアル調整 [DONE]
- [x] 親フレームの枠線 (`stroke-dasharray`) と子の進捗率の同期。
- [x] 親ラベルの配置最適化（重なり防止）。
- [x] `TreeView.css` へのグラスモフィズムとアニメーションの適用。

### Phase 4: ユーザーによる動的レイアウト調整 [DONE]
- [x] `spacingV/H`, `hPadding`, `hierarchyGap` を調整できるスライダーの実装。
- [x] 設定パネルの右下フローティング化（控えめなUIデザイン）。
- [x] 最適なデフォルト値（140px, 65px 等）の確定。

## 最終的な実装の詳細
- **モデル**: Pure Container Flow Model (親は器、子は実体)
- **UI**: 透過ガラススタイル + ダイナミック設定パネル
- **UX**: 直感的な依存関係の把握と、自分好みの余白調整機能
