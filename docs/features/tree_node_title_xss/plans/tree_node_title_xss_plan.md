# 実装プラン：Tree Node タイトル表示のXSSおよびレイアウト崩れ防止

## 1. 大まかな手順
1. `TreeView.jsx` のタイトルレンダリング部分の修正
2. 動作確認（XSSやレイアウト崩れが起きないかのテスト）

---

## 2. 手順の詳細化

### 2-1. `TreeView.jsx` のタイトルレンダリング部分の修正
- `src/components/features/tree/TreeView.jsx` を開く。
- 359行目付近にある `.html(d.data.title)` を検索する。
- 該当箇所を `.text(d.data.title)` に置換する。

### 2-2. 動作確認（テスト）
- アプリケーション上で、タスクのタイトルに意図的に `<h1>Test</h1>` や `<script>alert(1)</script>` などを入力する。
- リストビュー（ListView）およびツリービュー（TreeView）の両方で、入力した文字列がタグとして解釈されず、そのまま文字として表示されることを確認する。
- 画面のレイアウト崩れが起きていないことを確認する。

---

## 3. 次のステップ
- （全ての手順の詳細化が完了しました。ユーザーの承認を得て実装を開始します。）
